import { desoReactTemplate } from './deso-react-template';

describe('desoReactTemplate', () => {
  it('should work', () => {
    expect(desoReactTemplate()).toEqual('deso-react-template');
  });
});
