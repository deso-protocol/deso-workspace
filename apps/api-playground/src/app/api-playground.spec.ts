// import { PORT, runDefaultNodeApp } from './api-playground';
// describe('apiPlayground', () => {
//   it('should work', () => {
//     expect(runDefaultNodeApp()).toEqual(`app is running ${PORT}`);
//   });
// }); // doesn't make sense to test here, but going to leave this as an example
