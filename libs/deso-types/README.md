# deso-types

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build deso-types` to build the library.

## Running unit tests

Run `nx test deso-types` to execute the unit tests via [Jest](https://jestjs.io).
