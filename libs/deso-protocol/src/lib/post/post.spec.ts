// import axios from 'axios';
// import { Identity } from '../identity/Identity';
// import { Node } from '../Node/Node';
// import { MOCK_POST_NULL_BODY } from './post.mock';
// import { Posts } from './Posts';
// jest.mock('submitPost');
describe('posts', () => {
  //   beforeEach(() => {
  //     const open = jest.fn();
  //     const axiosMock = jest.spyOn(window, 'window', 'get');
  //     axiosMock.mockImplementation(() => {});
  //     Object.defineProperty(window, 'open', open);
  //     Object.defineProperties(axios, 'post');
  //   });
  //   it.only('should not allow null bodies in a post ', async () => {
  //     const node = new Node();
  //     const posts = new Posts(node, new Identity(node));
  //     const response = posts.submitPost(MOCK_POST_NULL_BODY);
  //     // console.log(response);
  //   });
  it('eventually have real tests', () => {
    expect('').toEqual('');
  });
});
